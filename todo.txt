install_landsacpe.sh

A 高

docker 某些源 不能安装

docker 安装失败的换源 未经测试


选择询问 在 landscape 中被使用的网卡，设为 manual。要怎么做才好呢？
增加多个 github 源
gitee 镜像加速


admin password 有冗余代码

B 低
alpine（printf 、openrc、换源、apk update/add）
centos 等
多语言
wan网卡配置，不一定要有，毕竟需要有网络才能安装，仅配置 dhcp client 或者 static ip 也行，懒得整了

webserver 相关功能未经测试，不见得有效


docker 安装函数里有个 curl 安装，是多余的





upgrade_landsacpe.sh

A 高

replace_files_with_rollback 函数中 关于 替换 handler 前 停止 docker 似乎有问题，没有在所有 handler 替换前 停止 docker，是否意味之着 只有 x86 musl 生效

怎么处理这个 docker 问题呢
将 handler 替换放到第一个，并且 在 handler 和 redirect.sh 替换成功后再 停止 landscape

可能使用不同的命名方式（太麻烦了不想整）
gitee 完善镜像加速？


B 低




redirect.sh

A 高


B 低

不是很重要，谁会装几十个容器？
# 3. REDIRECT_PKG_HANDLER_WRAPPER_REGION 
# master selver，master 执行 update 和 下载，selver 仅安装不下载，通过标志来确认状态？比如时间戳？ 将最新时间，在 一定时间范围内的文件 复制到 apt 对应目录中